# Copy this file to backend/.env and fill in real values (DO NOT COMMIT secrets)
DB_USER=piazzati_user
DB_PASSWORD=piazzati_password
DB_HOST=localhost
DB_PORT=5433
DB_NAME=db_piazzati

# Final expanded DATABASE_URL used by systemd EnvironmentFile (must be literal)
DATABASE_URL=postgresql://$DB_USER:$DB_PASSWORD@$DB_HOST:$DB_PORT/$DB_NAME
