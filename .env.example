# Copy this file to .env on the deployment host and set the real values.
# Important: Grafana needs a proper domain/IP so links and redirects don't use "localhost".
# Example:
# GRAFANA_DOMAIN=51.158.245.19
# or
# GRAFANA_DOMAIN=grafana.piazzati.example.com

# Used by docker-compose.yml / docker-compose.prod.yml to populate GF_SERVER_DOMAIN and GF_SERVER_ROOT_URL
GRAFANA_DOMAIN=localhost

# Other optional deployment vars you may want to set
GRAFANA_ADMIN_PASSWORD=changeme
GRAFANA_SECRET_KEY=change_this_to_a_long_random_value

# Backend/DB connection examples (fill if you use external grafana db)
# GRAFANA_DB_HOST=127.0.0.1
# GRAFANA_DB_NAME=grafana
# GRAFANA_DB_USER=grafana
# GRAFANA_DB_PASSWORD=secret
